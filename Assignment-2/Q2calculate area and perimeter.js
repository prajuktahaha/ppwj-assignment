
let floorLength = prompt("Enter the length of the floor:");
let floorWidth = prompt("Enter the width of the floor:");
let tileLength = prompt("Enter the length of a tile:");
let tileWidth = prompt("Enter the width of a tile:");
floor length = float(floor length);
floor width = float(floor width);
tile length = float(tile length);
tile width = float(tile width);
let floorArea = floorLength * floorWidth;
let tileArea = tileLength * tileWidth;
let numberOfTiles = float(floorArea / tileArea);
console.log("Number of tiles needed: " + float(numberOfTiles));
console.log("Perimeter = " , 2*(a+b));
